<div class="row">
    {{#each this}}
    {{#if playfabId}}
    <div class="col col-3 card text-center" style="margin: 0.5%">
        <div class="card-body">
            <h5 class="card-title">{{displayName}} : <i>{{playfabId}}</i></h5>
            <p class="card-text"><i>{{roomScene}}_{{roomZone}}#{{roomId}}</i></p>
        </div>
        <div class="card-footer text-muted">
            Created:&#09;{{createdAt}}
            <br />
            Updated:&#09;{{updatedAt}}
        </div>
    </div>
    {{/if}}
    {{/each}}
</div>